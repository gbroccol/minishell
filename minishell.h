#ifndef MINISHELL_H
# define MINISHELL_H

# include <unistd.h> // write, open, read, close
# include <stdlib.h> // malloc, free
# include <sys/types.h> // fork, wait, waitpid,
    // fork - 
    // wait - 
    // waitpid - 
# include <math.h>
# include <fcntl.h>
# include "./libft/libft.h"

//  wait3, wait4, signal, kill, exit, getcwd,
// chdir, stat, lstat, fstat, execve, dup, dup2, pipe,
// opendir, readdir, closedir, strerror, errno




#endif